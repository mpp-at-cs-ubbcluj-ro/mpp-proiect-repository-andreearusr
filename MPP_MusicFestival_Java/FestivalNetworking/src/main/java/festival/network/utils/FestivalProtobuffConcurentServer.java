package festival.network.utils;
/*
import services.IFestivalService;

import java.net.Socket;

public class FestivalProtobuffConcurentServer extends AbsConcurrentServer {

    private IFestivalService festivalServer;

    public FestivalProtobuffConcurentServer(int port, IFestivalService festivalServer) {
        super(port);
        this.festivalServer = festivalServer;
        System.out.println("Festival- FestivalProtobuffConcurrentServer");
    }

    @Override
    protected Thread createWorker(Socket client) {
        //ProtoFestivalWorker worker = new ProtoFestivalWorker(festivalServer, client);
        //Thread tw = new Thread(worker);
        //return tw;
    }
}
*/